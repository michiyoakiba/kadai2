<h1>Books</h1>

<div>
  <%= flash[:notice] %>
</div>

<table>
  <thead>
    <tr>
      <th>Tiile</th>
      <th>Body</th>
      <th colspan = "3"></th>
    </tr>
  </thead>
  <% @books.each do |book| %>
  <tbody>
    <tr>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <td>
     <%= link_to "Show", book_path(book.id) %>
    </td>
    <td>
      <%= link_to "Edit", edit_book_path(book.id) %>
    </td>
    <td>
      <%= link_to "Desrtoy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure？" %>
    </tr>
  </tbody>
  <% end %>
</table>



<h2>New book</h2>
<%= form_with model: @book, url: '/books', method: :post do |f| %>
  <h4>Tiile</h4>
  <%= f. text_field :title %>

  <h4>Body</h4>
  <%= f. text_area :body %>
</br>
  <%= f. submit 'Create Book' %>
<% end %>

